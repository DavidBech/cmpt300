#include <stdio.h>
#include <stdlib.h>

#include "list.h"

int main(int argc, char** argv){
    printf("[0]: %s \n", argv[0]);
    printf("[1]: %s \n", argv[1]);
    printf("[2]: %s \n", argv[2]);
    printf("[3]: %s \n", argv[3]);
    return(0);
}

